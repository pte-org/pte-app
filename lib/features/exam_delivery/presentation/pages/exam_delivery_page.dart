import 'package:flutter/material.dart';

/// Placeholder screen. Full exam UI (question rendering, navigation,
/// part transitions, timer display) is future work layered on top of the
/// Phase 6 Bloc skeleton — out of scope for this phase.
class ExamDeliveryPage extends StatelessWidget {
  const ExamDeliveryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Exam Delivery')));
  }
}
