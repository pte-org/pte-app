/// Throwaway service demonstrating this project's GetIt registration
/// convention (Phase 0). Delete this feature once Phase 1 provides a real
/// example to copy from.
class ExampleService {
  String greeting() => 'hello from example module';
}
