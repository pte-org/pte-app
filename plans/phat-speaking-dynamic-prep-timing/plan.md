# Plan: Dynamic, Audio-Duration-Aware Prep Timing for Speaking Audio-Prompt Screens
Status: 🟢 Complete — all 5 phases done, user-verified in live app
Date: 2026-08-31
Mode: Hard

## Overview
Replace the static per-task-*type* `prepSeconds` for the 5 audio-prompt Speaking task types with a per-item value computed server-side from the real uploaded audio's WAV-header duration, and add a one-shot poll to `TimerService` so recording starts promptly once that (often short) prep window genuinely ends — fixing both the label/progress-bar desync and the late-recording-start bugs found during `phat-speaking-audio-prompt-e2e`'s post-completion exploratory walkthrough at their shared root cause.

## Phases
- [x] Phase 1: media — WAV-only audio-prompt uploads + duration extraction — restrict content-type for audio-prompt uploads, compute+persist exact WAV duration at complete-upload, expose it via the existing internal audio-URL response, fail fast on unparseable files, and update the seed script to opt in.
- [x] Phase 2: exam-delivery — dynamic per-item prep timing — consume duration from the existing internal media call, move `preListenSeconds`/`preRecordSeconds` to server config (scoped to the 5 types, nullable elsewhere), compute `prepSeconds`/`prepDeadline` dynamically for the 5 audio-prompt types only.
- [x] Phase 3: pte-app — server-driven sub-stage constants — the 5 Speaking screens read `preListenSeconds`/`preRecordSeconds` (nullable client-side) from the task response instead of local hardcoded constants.
- [x] Phase 4: pte-app — TimerService one-shot deadline poll — add an anticipatory one-shot poll timed to the local countdown reaching zero, cancelling any still-pending periodic poll first so exactly one poll chain is ever active.
- [x] Phase 5: Manual E2E walkthrough — re-seed a known-duration WAV fixture, confirm label/progress-bar agreement and prompt recording start on a real local stack.

## Research Summary
Built entirely on findings already confirmed by source-reading + the brainstorm session (`plans/reports/260831-phat-speaking-dynamic-prep-timing-brainstorm.md`) and the requesting session's own direct source verification, further reconciled against a `plan-reviewer` WARN pass (findings incorporated below and in each phase file) — not re-derived here:

- **Bug 1 root cause (label/progress-bar desync):** `AudioListeningPrepCard` (`audio_prompt_record_body.dart`)'s countdown label already computes `audioSeconds = task.prepSeconds - preListenSeconds - preRecordSeconds` correctly in principle; it only disagrees with the real-audio-driven progress bar (`AudioPromptCubit`, from the prior plan) because `task.prepSeconds` is a static per-type number today, not derived from the real audio's actual duration. Once `prepSeconds` is computed server-side from real audio duration, this existing arithmetic is automatically correct — **no FE display-logic change needed**, confirmed by direct read of the widget.
- **Bug 2 root cause (late recording start):** `TimerService` (`timer_service.dart`) polls every fixed `_defaultPollInterval = Duration(seconds: 10)`; `phase` never self-advances locally between polls (deliberate "trust server exclusively" design, per its own class doc comment). For a short-prep audio-prompt task type, up to 10s worst-case staleness after the real `prepDeadline` is a large fraction of the whole window. User's chosen fix: one additional one-shot poll timed to the local countdown reaching zero, layered on the existing interval poll, reusing the already-injected `TimerScheduler` typedef (used today for both `_scheduleLocalTick` and `_pollTimer`) — not a new timer mechanism, not a lowered global interval. **Plan-reviewer HIGH finding, now resolved in Phase 4:** a naive implementation that lets the one-shot trigger the periodic poll's own self-rescheduling routine without first cancelling the still-pending `_pollTimer` leaves two parallel periodic-poll chains running — Phase 4 now explicitly cancels the pending periodic timer before the one-shot's reconciliation runs, with a dedicated unit test asserting exactly one active chain afterward.
- **Media service has no audio-processing library and no ffmpeg/ffprobe anywhere** (`pte-api/services/media/pom.xml`, shared `pte-api/Dockerfile`) — confirmed by direct inspection during brainstorm. Chosen approach: WAV-only audio-prompt uploads, duration read byte-exact from the WAV header via JDK-native `javax.sound.sampled` — zero new dependency. MP3/WebM audio-prompt support explicitly deferred (P3).
- **`PresignService.requestUpload`/`completeUpload`/`MediaObject` read directly** (`pte-api/services/media/.../service/PresignService.java`, `domain/MediaObject.java`): `RequestUploadRequest` today carries only `contentType` — there is **no existing signal distinguishing "this upload is a Speaking audio prompt for one of the 5 task types" from any other caller of the same generic `/objects` endpoint** (candidates' own recorded answers via `pte-app`'s `MediaRepositoryImpl`, or any other future use). Phase 1 resolves this by adding an explicit, opt-in signal (see Risks). `completeUpload` currently only flips status to `UPLOADED` — it never touches the uploaded bytes; a default (non-presigning) `MinioClient` bean already exists in `MinioConfig` but is currently unused anywhere in `PresignService`, and is the natural way to fetch the object stream for header parsing.
- **`seed-e2e.ps1` is the only real caller of the audio-prompt upload flow in this codebase** — no content-authoring UI exists for it. **Plan-reviewer HIGH finding, now resolved:** Phase 1 Step 7 explicitly updates that script's existing Repeat Sentence audio-upload call to opt into the new signal, so re-seeding after Phase 1 lands continues to produce a WAV-validated, duration-computed audio prompt instead of silently skipping both and breaking Phase 2's pin-time computation at Phase 5.
- **`SnapshotPinService.toPinnedItem`/`resolveAudioUrl` read directly** (`pte-api/services/exam-delivery/.../service/SnapshotPinService.java`) — the exact class the prior plan's Phase 1 already modified for audio-URL resolution. `resolveAudioUrl` already calls `mediaClient.presignGet(...)` once per item with a non-null `audioPromptRef`. **Plan-reviewer MEDIUM finding, now resolved:** rather than adding a second internal call purely to fetch duration (doubling pin-time network calls and failure surface for the same `MediaObject` row), Phase 1 extends the existing internal presigned-download response DTO with the duration field, and Phase 2 consumes it from that same, already-existing call. `PinnedItem.prepSeconds` is set once, per-item, at pin time from `TaskTimingConfig.timingFor(taskType)`; this is the single place dynamic computation must replace that static lookup for the 5 audio-prompt types (every other type: unchanged). Downstream, `TimerState.prepDeadline` is computed from `PinnedItem.prepSeconds` at task-start time (`AttemptMapper`'s doc comment) — so once `PinnedItem.prepSeconds` is dynamic, `prepDeadline` is automatically correct with no separate change needed.
- **`TaskTimingConfig`/`task-timing.json` read directly**: existing schema is `{prepSeconds, responseSeconds}` per task type, loaded once at startup, fails fast (`TaskTimingNotConfiguredException`) on an unconfigured type. Extending this same file's schema with optional `preListenSeconds`/`preRecordSeconds` fields (present only for the 5 audio-prompt types) is the most natural fit for the newly-server-owned sub-stage constants, per spec.md's own Assumptions — confirmed workable by direct read, not a new config surface.
- **`PinnedItem`/`PinnedItemView`/`TaskView`/`AttemptMapper` read directly**: none of these currently carry `preListenSeconds`/`preRecordSeconds` — all four need the new fields threaded through (entity → cache projection → student-facing DTO → mapper), mirroring exactly how `prepSeconds`/`responseSeconds` already flow today. **Plan-reviewer HIGH finding, now resolved:** these new fields must stay nullable/absent for every task type outside the 5 audio-prompt types (both server config and client `TaskView`) — Phase 2 and Phase 3 now explicitly agree on this (nullable on the client, populated only for 5 types on the server), correcting an earlier draft where Phase 3 had described them as required, which would have broken client-side deserialization for every other task type the moment Phase 3 shipped.
- **`MediaClient`/`InternalMediaController` read directly** (`pte-api/services/exam-delivery/.../client/MediaClient.java`, `pte-api/services/media/.../controller/InternalMediaController.java`): the existing internal `GET /internal/media-objects/{publicId}/presigned-url` service-to-service pattern (circuit-breaker-wrapped, `ROLE_INTERNAL_SERVICE`-protected) is reused as-is, with its response DTO extended (not replaced or duplicated) to also carry duration.
- **5 Dart screens read directly** (`pte-app/lib/features/exam_attempt/speaking_writing/presentation/pages/*.dart`): confirmed exact current hardcoded `preListenSeconds`/`preRecordSeconds` pairs — Repeat Sentence 3/3, Retell Lecture 3/10, Answer Short Question 3/3, Summarize Group Discussion 5/10, Respond to a Situation 20/10. Respond to a Situation's screen doc comment explicitly documents its 20s as a deliberate merge of 15s situation-reading + 5s audio-wait — must be preserved unchanged as that type's server-config `preListenSeconds`.
- **`timer_service_test.dart` read directly**: establishes the fake-`TimerScheduler`-capturing pattern (`_CapturingScheduler`, mirrors `proactive_refresh_scheduler_test.dart`'s `TimerFactory` convention) already used for both existing timers — Phase 4's new one-shot poll tests reuse this exact convention, not a new one, including a dedicated assertion that exactly one periodic-poll chain remains active after the one-shot fires.

## Dependencies
- Phase 5 needs a locally-running `pte-api` stack (`docker compose`) with every fix already landed by `phat-speaking-api-e2e-verify`/`phat-speaking-audio-prompt-e2e` (gateway URL fix, `MEDIA_URL` env var for `exam-delivery`, timer-phase-stuck-at-PREP fix, MinIO public-endpoint fix) — builds on that already-proven environment, not a fresh one.
- Phase 5 needs `pte-app` buildable/runnable against that local backend (Windows desktop target, per the prior plan).
- Phase 5 needs a WAV audio-prompt fixture with a specific, known, non-trivial duration — either the existing `pte-api/scripts/fixtures/repeat_sentence_sample.wav` (2.5s synthesized tone) if long enough to visually verify sync, or a freshly generated longer one.
- Phase 2 depends on Phase 1's new duration field/extended response existing; Phase 3 depends on Phase 2's new `TaskView` fields; Phase 4 is independent of Phases 1–3 (pure client-side `TimerService` change) but Phase 5 depends on all of Phases 1–4 being complete.
- Phase 5's re-seed depends on Phase 1 Step 7 (`seed-e2e.ps1` updated to opt into the new upload signal) having landed — without it, re-seeding produces an audio prompt with no duration and Phase 2's pin-time computation fails immediately.

## Risks
- HIGH: no existing signal on `RequestUploadRequest`/`/objects` distinguishes a Speaking-audio-prompt upload (WAV-only, duration-required) from any other caller of the same generic endpoint (candidates' own recorded answers, any future use) — **planner's call, pre-approved per spec.md's Assumptions, but flagged here for plan-reviewer/user visibility**: Phase 1 adds an explicit purpose/usage signal to the upload-request call (e.g. a new optional request field defaulting to the existing general-purpose behavior) so the WAV-only restriction applies only to audio-prompt uploads for the 5 task types, never narrowing the existing general `ALLOWED_CONTENT_TYPES` path candidates' own recordings use. Mitigation: Phase 1 includes a regression test confirming the existing (non-audio-prompt) upload path is completely unaffected.
- HIGH: `completeUpload` currently never touches the uploaded object's bytes — reading the WAV header requires a real MinIO round-trip at complete-upload time that doesn't exist today, using the already-defined but currently-unused default `MinioClient` bean — Mitigation: Phase 1 adds unit tests for valid/corrupt/non-WAV cases against this new read path specifically, not just the content-type gate.
- HIGH (plan-reviewer finding, resolved): `seed-e2e.ps1` is the only real caller of the audio-prompt upload flow in this codebase — if its upload call isn't updated to opt into Phase 1's new signal, re-seeding silently skips WAV validation/duration computation and Phase 2's pin-time duration lookup fails loudly at Phase 5's very first walkthrough attempt. Mitigation: Phase 1 Step 7 explicitly updates the script's existing upload call as part of Phase 1, not deferred to Phase 5.
- HIGH (plan-reviewer finding, resolved): a naive one-shot-poll implementation (Phase 4) that reuses the periodic poll's own self-rescheduling routine without first cancelling the still-pending `_pollTimer` leaves two independent periodic-poll chains running in parallel for the rest of the task, silently doubling `fetchTimerState` traffic. Mitigation: Phase 4 Steps 2–3 explicitly cancel the pending periodic timer before the one-shot's reconciliation/reschedule runs, verified by a dedicated unit test asserting exactly one active chain afterward.
- HIGH (plan-reviewer finding, resolved): `preListenSeconds`/`preRecordSeconds` cannot be modeled as required/non-nullable on the client (`TaskView`) while the server only ever populates them for 5 of ~20 task types (Phase 2's config is explicitly scoped to those 5) — a hard non-null cast would break deserialization for every other task type's response the moment Phase 3 shipped. Mitigation: reconciled as nullable (`int?`) on the client — Phase 2's config/response stays scoped to the 5 types, Phase 3's 5 Speaking screens assert non-null only at their own read sites; every other task type never reads these fields at all.
- MEDIUM: **exact server-config location for `preListenSeconds`/`preRecordSeconds`** — planner's chosen approach is extending `task-timing.json`'s existing per-task-type schema (optional fields, present only for the 5 audio-prompt types) rather than a new config surface, per spec.md's own Assumptions (pre-approved as "planner's call," flagged here for visibility per this plan's explicit instructions). Mitigation: Phase 2 keeps this change additive and backward-compatible — every other task type's config entry is untouched, `TaskTimingConfig.Timing` gains nullable fields rather than replacing existing ones.
- MEDIUM (plan-reviewer finding, resolved): a naive design would have exam-delivery make two separate internal media calls per audio-prompt item at pin time (the existing URL-resolution call, plus a new duration-only call) — same `mediaPublicId`, same tenant, same call site, doubling network calls and failure surface for no benefit. Mitigation: Phase 1 extends the existing internal presigned-download response DTO with an optional duration field instead of adding a new endpoint; Phase 2 consumes duration from that same existing call.
- MEDIUM: `task-timing.json`'s existing static `prepSeconds` for the 5 audio-prompt types (added by `phat-speaking-audio-prompt-e2e` Phase 1) becomes dead/superseded once dynamic computation ships for those types — Mitigation: Phase 2 leaves the static values in place as an inert, unused fallback (documented as such in the config's own comment) rather than removing them, since `responseSeconds` in that same entry is still read normally (only `prepSeconds` for these 5 types stops being consulted).
- MEDIUM: widening the WAV-only restriction must not affect candidates' own recorded-answer uploads (already WAV-only via `AudioRecorderServiceImpl`, a separate content-type path per spec.md's own Assumptions) — Mitigation: Phase 1's regression test explicitly exercises that path unchanged.
- LOW (plan-reviewer NOTED, resolved): the missing-duration guard (Phase 2 Step 5) needs an explicitly-named exception rather than an ad-hoc mid-cook decision — mirroring the existing `MissingAudioPromptException` precedent (not `AudioResolutionFailedException` — that one's 503 is for a call failure, this is a data-completeness problem), Phase 2 names its own new exception `MissingAudioDurationException` at `HttpStatus.UNPROCESSABLE_ENTITY` (422).
- HIGH (2nd-pass plan-reviewer finding, resolved): Phase 1's Step 6 originally described "the existing internal presigned-download response DTO" as one shared type — it's actually **two separate record classes**, media-service's own `PresignedDownloadResponse` and exam-delivery's independently-maintained mirror `MediaClient`'s `MediaPresignedDownloadResponse` (explicitly documented in its own file as "not a shared class"). Extending only one would leave Phase 2's consuming code looking for a field that doesn't exist on its side. Mitigation: Phase 1 Step 6 now explicitly names both classes and requires both to gain the new field.
- MEDIUM (2nd-pass plan-reviewer finding, resolved): Phase 4's originally-described regression test ("reuse the existing fake-scheduler convention to assert exactly one active periodic chain") can't actually detect the bug it's meant to catch — the existing `_CapturingScheduler` test double only captures raw `(duration, callback)` pairs, not the `Timer` objects returned, so a test built on re-invoking captured closures bypasses real `Timer.cancel()` semantics entirely and would pass even if the cancellation fix were later silently removed. Mitigation: Phase 4 Step 7 now explicitly extends `_CapturingScheduler` to also capture and expose the returned `Timer` objects, so the regression test can assert real `.isActive == false` on the previously-active periodic timer.
- LOW: dev/test audio-prompt `MediaObject`s created before this ships (e.g. the prior plan's Repeat Sentence fixture) have no stored duration — per spec.md's own Assumptions, these are expected to be re-seeded fresh, not migrated; Phase 5 re-seeds (via the now-updated `seed-e2e.ps1`) rather than reusing old data.
- LOW: Phase 5's fixture must have a genuinely audible, non-trivial, precisely-known duration for the sync to be visually/aurally verifiable — resolved (user confirmed): Phase 5 generates a new, longer (~5-6s) fixture rather than reusing the existing 2.5s tone, which risked being too short to distinguish "in sync" from "coincidentally close."

## Session Notes
<!-- Updated by cook automatically — do not edit manually -->

**Last active:** 2026-09-01 00:34
**Phase in progress:** none — all 5 phases complete
**Status:** ALL PHASES COMPLETE. User walked through both bugs live in the app and confirmed both fixed. Code-reviewer found 1 real HIGH race-condition bug (fixed + regression-tested, 517/517 passing). Plan ready for git-manager (stage only, per standing instruction).

### Decisions made this session
- **Phase 1:** `RequestUploadRequest` gained a nullable `Boolean audioPrompt` field (opt-in signal) rather than a `purpose` string enum — simplest thing that satisfies the requirement, easy to extend later if a second purpose ever needs distinguishing.
- **Phase 1:** `PresignService` now injects the previously-unused default `minioClient` bean (by-parameter-name Spring autowiring against the bean literally named `minioClient` — no `@Qualifier` needed since only `presignMinioClient` needs one to disambiguate).
- **Phase 1:** Split WAV duration logic into two methods for testability: `extractWavDurationSeconds(String storageKey)` (fetches from MinIO, package-private so `Mockito.spy` can stub it directly rather than needing to fabricate a real MinIO SDK `GetObjectResponse`) and `parseWavDurationSeconds(InputStream)` (the actual header-parsing math, unit-tested directly against real generated WAV bytes — not in the plan's original phase text, but a natural refactor to make the fetch-vs-parse concerns independently testable without mocking a MinIO SDK type).
- **Phase 1:** Duration extraction happens *before* `markUploaded()`/`save()` in `completeUpload`, not after — so a parse failure never touches persisted state at all (belt-and-suspenders beyond just relying on `@Transactional` rollback).
- **Phase 1:** Reused `UnsupportedContentTypeException` (already existing, HTTP 400) for the "audio-prompt upload with a non-WAV content type" case, rather than a new exception — same actual failure category (content type rejected), just a narrower allow-list depending on context.
- **Phase 1:** `HttpStatus.UNPROCESSABLE_ENTITY` triggers a Spring deprecation warning at compile time — pre-existing in this codebase (the sibling `MissingAudioPromptException` already uses the identical pattern) — left as-is to match established convention, not fixed as part of this phase.
- **Phase 2 (design refinement beyond the plan's original text):** `SnapshotPinService` doesn't key its dynamic-vs-static prep-timing branch off a hardcoded `Set<String>` of the 5 task-type names — it checks `timing.preListenSeconds() != null` instead, since config *presence* is already the real signal and duplicating a task-type list in Java would be a second, driftable source of truth for the same fact. Simpler, and matches `TaskTimingConfig.Timing`'s own new doc comment.
- **Phase 2:** `task-timing.json`'s 5 audio-prompt entries now carry `preListenSeconds`/`preRecordSeconds` matching the FE's exact previous hardcoded values (Repeat Sentence 3/3, Retell Lecture 3/10, Answer Short Question 3/3, Respond to a Situation 20/10, Summarize Group Discussion 5/10) — their own `prepSeconds` stays as an inert, unused fallback per the user's confirmed choice.
- **Phase 2:** `SnapshotPinService.resolveAudioUrl` now returns `Integer` (the resolved duration) instead of `void` — the single existing `mediaClient.presignGet` call now serves both the URL-resolution need (prior plan) and the duration need (this plan), exactly as the plan-reviewer's MEDIUM finding required. LISTENING's call site ignores the return value; the dynamic-prep branch below it uses it.
- **Phase 2:** Confirmed by direct source read (no code change needed): `TimerService.startTaskTimer` already computes `prepDeadline` from `PinnedItemView.prepSeconds()`, and `AttemptService`'s exam-end-time summation already reads `PinnedItem.getPrepSeconds()` directly — both automatically pick up the new dynamic value with zero separate change, exactly as plan.md's Risks anticipated.
- **Phase 2:** Existing tests (`AttemptMapperTest`, `TimerServiceTest`, `SnapshotPinServiceTest`) needed constructor-arity fixes for the widened `PinnedItemView`/`TaskTimingConfig.Timing` records (mechanical, no assertions changed) — plus new dedicated tests added directly to those same files for Phase 2's own requirements (all 5 audio-prompt types' dynamic computation, Respond to a Situation's preserved value, Read Aloud regression, the missing-duration exception, and `TaskView` field flow-through).
- **Phase 3:** `TaskView` gained nullable `int? preListenSeconds, preRecordSeconds` (constructor + `fromJson`); all 5 Speaking screens' module-level `const int _preListenSeconds`/`_preRecordSeconds` constants were removed and every call site switched to `task.preListenSeconds!`/`task.preRecordSeconds!` (or `widget.task....` where accessed from `State`), each with a comment explaining why the null-assertion is safe (server only ever pins these 5 types with both populated). `RetellLectureScreen`'s `_instructionText` gained a `preRecordSeconds` parameter since it interpolates that value into its instruction string.
- **Phase 3:** `AudioPromptRecordBody`/`AudioListeningPrepCard`/`RecordedAnswerPrepCard` were **not** changed — they already took `preListenSeconds`/`preRecordSeconds` as explicit constructor params (not read from `task` internally), so only their 5 screen callers needed updating, not the shared widgets themselves.
- **Phase 3:** Updated 6 test fixtures to supply the now-required values instead of relying on removed hardcoded constants: the 5 screens' own `_xxxTask()` widget-test fixtures, plus `task_type_dispatcher_test.dart`'s 5 matching fixtures (its routing tests pump the real screens, so they hit the same null-assertions). `audio_prompt_record_body_test.dart` and `audio_prompt_cubit_test.dart` needed **no** change — both pass `preListenSeconds`/`preRecordSeconds` explicitly as constructor/widget params rather than reading them off a `TaskView` fixture.
- **Phase 3:** `flutter analyze` (197s, correct `pte-app` directory confirmed) — 0 issues. Full `flutter test` — 510/510 passing, no regressions.
- **Phase 4:** `TimerService` gained `_oneShotPollTimer`, `_attemptPublicId` (doubles as the "polling is active" flag), and `_pollInterval` fields, plus a new `_scheduleOneShotPoll(int generation)` method. Called from `startPolling` (initial arm, using `currentSnapshot.remaining` right after the caller's preceding `seedFromTask`) and from the tail of `reconcileFromServer` (re-arm on every fresh reconciliation, per Step 4) — **not** from the shared private `_seed`, since `_seed` alone (bare `seedFromTask`, before `startPolling`) has no `attemptPublicId`/active poll to reconcile against yet.
- **Phase 4:** `_scheduleOneShotPoll` is a guarded no-op whenever `_attemptPublicId` is null — this is what keeps every pre-existing `reconcileFromServer`-only unit test (no `startPolling` call) from suddenly scheduling a stray real `Timer`; verified by a dedicated new test asserting `scheduler.callbacks` stays empty for that exact call pattern.
- **Phase 4:** The one-shot's fired callback cancels `_pollTimer` (`.cancel()` + null out) **before** calling `_poll(...)` again — `_poll`'s own tail-end rescheduling then becomes the only live periodic chain afterward, satisfying the plan-reviewer's cancel-before-reconcile ordering requirement without a second, parallel timer-management pattern.
- **Phase 4:** `stop()` now also cancels `_oneShotPollTimer` and nulls both it and `_attemptPublicId` — a task transition (`startPolling` calling `stop()` first) or an explicit `stop()`/`dispose()` always leaves zero orphaned one-shot callbacks capable of doing anything (the stale-generation guard inside the callback is the actual safety net; nulling the fields is belt-and-suspenders).
- **Phase 4 (2nd-pass plan-reviewer finding, addressed):** `_CapturingScheduler` (`timer_service_test.dart`) extended with a `timers` list capturing the real `Timer` object returned per call, not just the raw `(duration, callback)` pair — the new "exactly one active periodic chain" regression test asserts genuine `Timer.isActive` transitions (`true` → `false` on the old periodic timer, a fresh `true` on the new one), which closure re-invocation alone cannot distinguish from a silently-removed cancellation.
- **Phase 4:** 5 new tests added to `timer_service_test.dart`: (1) the one-shot is scheduled at `startPolling` targeting the current countdown and firing it triggers a fresh reconciliation without waiting for the periodic interval; (2) firing the one-shot leaves exactly one active periodic chain (the dedicated `.isActive` regression test); (3) a fresh reconciliation re-arms/replaces the previously-scheduled one-shot; (4) `stop()` leaves no orphaned one-shot callback capable of firing; (5) `reconcileFromServer` called directly with no active polling schedules nothing. All existing `timer_service_test.dart` tests pass unmodified — 19/19 in that file, 515/515 in the full suite (up from 510, zero regressions). `flutter analyze` — 0 issues.

- **Phase 5 (real bug found + fixed, beyond this plan's original scope):** `MediaObject.audioPrompt` was declared `@Column(nullable = false)` on a `boolean` with no DEFAULT — against the dev database's already-existing `media_objects` rows, `ddl-auto: update`'s own `ALTER TABLE ... ADD COLUMN audio_prompt boolean not null` failed outright (`column "audio_prompt" ... contains null values`), silently leaving the column entirely absent and every subsequent upload 500ing (`column "audio_prompt" ... does not exist`) — a genuine schema-migration bug that would hit any other dev pulling this code onto an existing local database, not merely this walkthrough's own friction. Fixed by adding `columnDefinition = "boolean not null default false"` to the `@Column` annotation, which lets Hibernate emit an explicit `DEFAULT false` the `ALTER TABLE` can satisfy against existing rows; rebuilt+restarted the `media` container and confirmed the column now exists with that default.
- **Phase 5 Step 1:** Generated `pte-api/scripts/fixtures/repeat_sentence_sample_long.wav` — a 5.5s synthesized 440Hz tone (mono, 16-bit, 16kHz, matching the existing 2.5s fixture's format, with a 50ms fade in/out to avoid a click) via Python's stdlib `wave` module, same generation approach as the existing fixture. Committed alongside (not replacing) the existing `repeat_sentence_sample.wav`.
- **Phase 5 Step 2:** Rebuilt+restarted the `media` and `exam-delivery` containers with all of Phases 1-4's code (they'd been running pre-Phase-1 images all session). Re-ran `seed-e2e.ps1 -RepeatSentenceAudioFixturePath 'scripts/fixtures/repeat_sentence_sample_long.wav'` — the script's own documented limitation (no "get snapshot/question by name" API) meant the old, already-published Repeat Sentence question/blueprint (from the prior plan, pointing at the old 2.5s media with no stored duration under the old pre-Phase-1 schema) couldn't be cleanly superseded by clearing state-file fields alone; resolved by passing distinct `-RepeatSentenceQuestionTitle`/`-RepeatSentenceBlueprintName`/`-RepeatSentenceSessionName` overrides (`... Phase5`) to force genuinely fresh creation against the new media object, leaving the old orphaned question/blueprint harmlessly in place rather than deleting/migrating it (matches this plan's own Risk: "expected to be re-seeded fresh, not migrated").
- **Phase 5 Step 3 (API-level verification, PASSED):** Started an attempt directly via `POST /api/exam-delivery/attempts` against the freshly-seeded Repeat Sentence session — response confirmed `prepSeconds: 12` = `preListenSeconds(3) + preRecordSeconds(3) + audioDuration`, and a direct DB check of the `media_objects` row confirmed `audio_prompt: true, duration_seconds: 6` — exactly `Math.round(5.5s)`, matching `PresignService.parseWavDurationSeconds`'s rounding exactly. The dynamic per-item computation is confirmed correct end-to-end at the API level before ever involving the app.
- **Phase 5 (process note, applying the prior plan's own documented lesson):** the attempt started for Step 3's API verification consumed that session's one-shot slot (`ExamAttempt` is one-shot per student+session) — rather than hand the user that same now-in-progress, likely-already-expired-by-the-time-they-look session, seeded a second, genuinely untouched session (`E2E Seed Session - Repeat Sentence Phase5 Walkthrough`) reserved purely for the user's own Steps 4-5 walkthrough. Confirmed via direct DB query that the existing Read Aloud session (reused for Step 6's regression spot-check) has zero prior attempts despite its age — safe to hand to the user as-is.

- **Phase 5 Steps 4/5 (live app walkthrough, Bug 2 PASS, Bug 1 initially INCOMPLETE then fixed):** user tested session `5660c941-...` (Repeat Sentence, 6s audio window). Bug 2 (recording start lag) confirmed fixed immediately — recording started within ~1s of the Record card's "Beginning in 0 seconds". Bug 1 was only *partially* fixed: user's screenshot showed the label correctly reading "Playing 0 seconds left" but the progress bar visibly short of full (not reaching the right edge). Root-caused to real playback start latency (presigned-URL fetch + buffer) meaning the real audio player's position can still trail `duration` by that same lag once the *theoretical* elapsed-time window (which assumes zero start latency) has already closed. **Fix:** `AudioListeningPrepCard` (`audio_prompt_record_body.dart`) now forces `progress: 1.0` whenever `audioRemaining <= 0`, instead of trusting the cubit's real (possibly-still-catching-up) position. Re-verified in a fresh session (`4a1fa402-...` then `347d30da-...`) — user confirmed fixed ("ổn rồi").
- **Phase 5 (follow-up UX polish, user-requested, applied):** user asked for the progress bar to fill smoothly/evenly per second proportional to audio length, rather than visibly jumping between updates. The underlying `position/duration` math was already linear/duration-proportional — the visual jumpiness came from `LinearProgressIndicator` snapping instantly to each new value with no interpolation between the native player's position-stream emissions. Fixed by wrapping it in `TweenAnimationBuilder<double>` (300ms, `Curves.linear`) in `AudioListeningStatusCard` — it self-updates its animation's start point to wherever it currently sits on each new target, so this stays correct across arbitrarily many updates, not just the first. 2 widget tests in `audio_prompt_record_body_test.dart` updated to `await tester.pumpAndSettle()` before reading `.value` (first frame now renders the tween's `begin`, not its target). `audio_listening_status_card_test.dart` unaffected (asserts no `.value`). Verified: `flutter analyze` 0 issues, full suite 516/516.
- **Phase 5 Step 6 (regression spot-check, PASS):** user confirmed Read Aloud's prep/response timing behaves normally, no regression.
- **Phase 5 (new finding, deferred — NOT fixed this phase, out of this plan's scope):** while testing, user's terminal showed repeated `ConflictException: ATTEMPT_ALREADY_COMPLETE` from `TimerService._poll` — an attempt that has already completed keeps getting polled every 10s indefinitely (server correctly 409s, client silently logs+retries per its existing "never block on one failed poll" design, so no crash/UI break). This is **pre-existing behavior predating this plan** (not caused by Phase 4's one-shot poll addition) — `TimerService` has no signal today to stop polling once an attempt reaches a terminal state. Confirmed by the user as non-blocking (prep/response timing itself was unaffected). Logged here as a candidate for a future small follow-up plan, same pattern as the original Bug 1/Bug 2 findings from the prior plan.
- **Post-completion code-reviewer pass (1 real HIGH finding, fixed):** a full `code-reviewer` sub-agent review across both repos, after all 5 phases + the walkthrough-discovered fixes above, found `TimerService._poll`'s own tail-end periodic-timer scheduling (`_pollTimer = _scheduler(interval, ...)`) was **not symmetric** with `_scheduleOneShotPoll`'s cancel-before-overwrite pattern. Concretely: if the one-shot fires and re-enters `_poll` while the *original* `startPolling`-triggered `_poll` call is still awaiting a slow/cold `fetchTimerState` (plausible precisely because the one-shot targets short prep windows — a slow round-trip can take as long as the whole window), both calls independently reach the tail-end line with `_pollTimer` still `null` (the cancel-before-overwrite guard in the one-shot's own firing path only helps if `_pollTimer` was already set). Whichever of the two racing calls finishes last then silently overwrites `_pollTimer` with no cancellation of the other — leaking the earlier one, still armed and firing on its own schedule, reproducing the exact duplicate-periodic-chain bug Phase 4 was built to eliminate, via a different trigger than the one Phase 4's original tests covered (all 5 of which `pumpEventQueue()`'d before firing the one-shot, i.e. only ever exercised the case where the original `_poll` had *already* completed). **Fix:** `_poll`'s tail now also does `_pollTimer?.cancel();` before reassigning, exactly mirroring `_scheduleOneShotPoll`. **Regression test added:** `timer_service_test.dart` — uses a `Completer` to hold the original call's fetch open, fires the one-shot mid-flight (deliberately no `pumpEventQueue()` first), resolves the one-shot's own (faster) fetch, then resolves the original slow fetch, and asserts the one-shot-installed periodic `Timer.isActive` becomes `false` only once the original call's tail runs — i.e. the fix, not just closure re-invocation. Verified: `flutter analyze` 0 issues, full suite 517/517 (516 + 1 new).

### Next immediate action
**All 5 phases complete, user-verified end-to-end in the live app, code-reviewed with 1 real finding fixed.** Proceed with `git-manager` to stage the changes (stage only — commit/push only when the user explicitly asks, per standing instruction). `project-manager`/`docs-manager` passes are lightweight given `plan.md` itself already carries the full record — confirm nothing else needs syncing (README/API docs) before staging.