# Phase 2: AutoRecordCubit/AutoRecordState Relocation

## Requirements
Port the shared `AutoRecordCubit`, its `AutoRecordState`, and the `UploadTrackingState`/`RecordingPhase`/`AutoAdvanceOnUploadReady` primitives it depends on into `speaking_writing/presentation/cubit/` and `speaking_writing/presentation/widgets/`, matching dev's package structure and absolute-import convention, with both CRITICAL guards verified intact at the new location.

## Steps
1. Resolve `lib/features/exam_attempt/speaking_writing/presentation/cubit/read_aloud_cubit.dart`'s import-block conflict: adopt dev's `package:pte_app/...` style, pointing the state import at the new `speaking_writing/presentation/cubit/auto_record_state.dart` (created in step 2) and the recorder-service import at `speaking_writing/domain/audio_recorder_service.dart`; leave the class body (everything from `resolveRecordingFilePath` through `AutoRecordCubit`'s closing brace) byte-for-byte unchanged.
2. Create `lib/features/exam_attempt/speaking_writing/presentation/cubit/auto_record_state.dart` by rewriting dev's current stale content at `lib/features/exam_attempt/speaking_writing/presentation/cubit/read_aloud_state.dart` (old pre-unification `ReadAloudState`, doesn't implement `UploadTrackingState`) into local's actual `AutoRecordState` shape — read `lib/features/exam_attempt/presentation/cubit/auto_record_state.dart` for the exact target (class `AutoRecordState extends Equatable implements UploadTrackingState`, `recordingPhase`/`uploadStatus` fields, `copyWith`, `export 'recording_phase.dart'`) — then delete the old `read_aloud_state.dart` file it replaces.
3. Relocate `lib/features/exam_attempt/presentation/cubit/{upload_tracking_state.dart,recording_phase.dart}` to `lib/features/exam_attempt/speaking_writing/presentation/cubit/`, updating their own imports (`upload_tracking_state.dart`'s single import) to `package:pte_app/...` style.
4. Relocate `lib/features/exam_attempt/presentation/widgets/auto_advance_on_upload_ready.dart` to `lib/features/exam_attempt/speaking_writing/presentation/widgets/auto_advance_on_upload_ready.dart`, updating its imports (core storage/sync, `exam_attempt_bloc`/`exam_attempt_event` stay at the shared shell path, `upload_tracking_state.dart` now points at its new `speaking_writing/presentation/cubit/` location) to `package:pte_app/...` style.
5. Delete the now-fully-superseded old flat-path files: `lib/features/exam_attempt/presentation/cubit/{auto_record_cubit.dart,auto_record_state.dart,upload_tracking_state.dart,recording_phase.dart}`, `lib/features/exam_attempt/presentation/widgets/auto_advance_on_upload_ready.dart`, and the very-old pre-unification `lib/features/exam_attempt/presentation/cubit/read_aloud_cubit.dart` (deleted on both sides, git-rm only, no content to merge). Note: `auto_record_cubit.dart` is itself one of this plan's 11 marker-carrying conflicted files, not an already-clean deletion — its conflict is the same import-block-only rename-conflict mirrored at the new-path file resolved in step 1 (byte-identical class body), so deleting it here is equivalent to resolving-then-deleting, not a content-loss risk.
6. Update `test/unit/features/exam_attempt/auto_record_cubit_test.dart`'s import to point at the new `speaking_writing/presentation/cubit/` location (file itself stays at its current flat `test/` path — no test-directory reorganization, matching dev's own convention of keeping `test/` flat). Update `test/widget/features/exam_attempt/auto_advance_on_upload_ready_test.dart`'s import similarly. Delete `test/unit/features/exam_attempt/read_aloud_cubit_test.dart` outright (superseded by `auto_record_cubit_test.dart`, per user decision — resolve its conflict by deletion, not by merging content).

## Success Criteria
- `Glob lib/features/exam_attempt/presentation/cubit/*.dart` no longer lists `auto_record_cubit.dart`, `auto_record_state.dart`, `upload_tracking_state.dart`, `recording_phase.dart`, or `read_aloud_cubit.dart`.
- `Glob lib/features/exam_attempt/speaking_writing/presentation/cubit/*.dart` lists `read_aloud_cubit.dart`, `auto_record_state.dart`, `upload_tracking_state.dart`, `recording_phase.dart`, `write_essay_cubit.dart`, `write_essay_state.dart`.
- `lib/features/exam_attempt/speaking_writing/presentation/cubit/auto_record_state.dart` declares `class AutoRecordState extends Equatable implements UploadTrackingState` (not `ReadAloudState`) with `recordingPhase`/`uploadStatus` fields and `copyWith`.
- **CRITICAL GATE**: `grep -n "_startInFlight\|_stopInFlight" lib/features/exam_attempt/speaking_writing/presentation/cubit/read_aloud_cubit.dart` returns both field declarations and both usages inside `onTimerSnapshot`, and the full multi-paragraph doc comment directly above `bool _startInFlight = false;` (starting "In-flight guards for the auto-record path...") is present verbatim — diff this doc comment block against the pre-merge local file to confirm zero truncation.
- `dart analyze lib/features/exam_attempt/speaking_writing/presentation/cubit/ lib/features/exam_attempt/speaking_writing/presentation/widgets/auto_advance_on_upload_ready.dart` is clean.
- `test/unit/features/exam_attempt/read_aloud_cubit_test.dart` no longer exists; `auto_record_cubit_test.dart` and `auto_advance_on_upload_ready_test.dart` import from the new `speaking_writing/` paths.

## Risks
- Rewriting `read_aloud_state.dart` into `auto_record_state.dart` by hand risks silently reintroducing the old `ReadAloudState` shape (missing `implements UploadTrackingState`) if the file is edited in place instead of replaced wholesale — Mitigation: write the new file's full content from the reference `presentation/cubit/auto_record_state.dart`, don't patch dev's stale version incrementally.
- `auto_advance_on_upload_ready.dart`'s generic bound (`S extends UploadTrackingState`) silently type-checks against the wrong `UploadTrackingState` if two copies of the interface end up on the import path simultaneously mid-phase — Mitigation: step 5's deletion of the old flat-path `upload_tracking_state.dart` must land in the same edit pass as step 4's relocation, not a separate later cleanup.
