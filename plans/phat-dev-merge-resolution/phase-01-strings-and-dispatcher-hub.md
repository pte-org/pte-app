# Phase 1: Strings & Task-Type Dispatcher Hub

## Requirements
Resolve the shared-infrastructure conflicts that every other phase's files depend on: the string classes (`AppStrings`, `ExamAttemptStrings`, `SpeakingWritingStrings`) and `TaskTypeDispatcher` (+ its test), so downstream phases have a stable, decided target to import against instead of re-deriving naming/routing decisions per file.

## Steps
1. Resolve `lib/core/constants/app_strings.dart`: keep dev's reduced class (`appTitle` only), remove all conflict markers.
2. Diff local's removed `AppStrings` constants against `lib/features/exam_attempt/constants/exam_attempt_strings.dart`'s current content; add only `writeEssayTextFieldLabel` if not already covered by `SpeakingWritingStrings` — everything else shell-level (`sessionEntryTitle`, `taskAdvanceButtonLabel`, `wordCount*`, `unsupportedTaskTypePrefix`) is already present in dev's `ExamAttemptStrings`, confirm rather than re-add. Do **not** migrate `examBrandLine1`/`examBrandLine2`/`examBrandLine3`/`examTimeRemainingLabel` — their only consumers are local's `_BrandBlock`/`_CountdownBlock` in `exam_app_bar.dart`, which step 5 replaces wholesale with dev's version; migrating them here would only create 4 dead constants immediately contradicted by step 5.
3. Add every speaking/read-aloud/describe-image/repeat-sentence/recording/audio-listening constant from local's removed `AppStrings` into `lib/features/exam_attempt/speaking_writing/constants/speaking_writing_strings.dart`, using local's generic `recording*`-prefixed names (`recordingCurrentStatusLabel`, `recordingBeginningInPrefix/Suffix`, `recordingInProgressPrefix/Suffix`, `recordingStillUploadingLabel`, `recordingUploadReadyLabel`) since the status card these back is shared across Read Aloud, Repeat Sentence, and Describe Image — not read-aloud-specific.
4. Remove dev's now-orphaned manual-button-only constants from `SpeakingWritingStrings` (`readAloudStartRecordingLabel`, `readAloudStopRecordingLabel`, `readAloudRecordingIndicator`, `readAloudNotRecordedYetLabel`, `readAloudStillUploadingLabel`, `readAloudUploadReadyLabel`) — their only consumers (dev's old manual `ReadAloudScreen`/`ReadAloudCubit`/`ReadAloudAdvanceButton`) are being deleted in Phases 2–3; keep `writeEssayTextFieldLabel` (still used by `WriteEssayScreen`).
5. Resolve `lib/features/exam_attempt/presentation/widgets/exam_app_bar.dart`: take dev's body (force-submit affordance, `BlocSelector` + `_phaseLabel`/`_formatRemaining`), dev's `package:` import style, `ExamAttemptStrings` references — this fully replaces local's `_BrandBlock`/`_CountdownBlock` layout since dev's version is a superset (phase label + countdown + force-submit vs. local's brand block + countdown only); confirm no `AppStrings` reference survives.
6. Resolve `lib/features/exam_attempt/presentation/widgets/exam_bottom_bar.dart`: trivial hand-merge — take dev's added `timer_phase.dart`/bloc imports alongside the existing container body (no behavioral conflict, both sides' import lists simply union).
7. Resolve `lib/features/exam_attempt/presentation/widgets/task_type_dispatcher.dart`: union the import block (dev's `package:` style + new subpackage paths for reading/listening screens, plus forward-looking imports for `speaking_writing/presentation/pages/{read_aloud,repeat_sentence,describe_image}_screen.dart` — those files won't physically exist at that path until Phase 3, so `flutter analyze` on this file is expected to still fail after this step; that's normal transient state, not a Phase 1 regression); union the switch body (local's `REPEAT_SENTENCE`/`DESCRIBE_IMAGE` cases + dev's 8 Listening cases, unchanged from each side); keep the `audioPlayerService` constructor parameter and its doc comment, threaded only into the 8 Listening screen constructors.
8. Resolve `test/widget/features/exam_attempt/task_type_dispatcher_test.dart`: union both sides — keep local's `_FakePathProviderPlatform`, `_repeatSentenceTask`/`_describeImageTask` fixtures and the `REPEAT_SENTENCE`/`DESCRIBE_IMAGE` routing test groups, plus dev's `_MockAudioPlayerService`; add `audioPlayerService: audioPlayerService` to the shared `buildSubject` helper's `TaskTypeDispatcher(...)` call.

## Success Criteria
- `grep -rn "<<<<<<<\|=======\|>>>>>>>" lib/core/constants/app_strings.dart lib/features/exam_attempt/presentation/widgets/{task_type_dispatcher,exam_app_bar,exam_bottom_bar}.dart lib/features/exam_attempt/constants/exam_attempt_strings.dart lib/features/exam_attempt/speaking_writing/constants/speaking_writing_strings.dart test/widget/features/exam_attempt/task_type_dispatcher_test.dart` returns no matches.
- `dart analyze lib/core/constants/app_strings.dart lib/features/exam_attempt/constants/exam_attempt_strings.dart lib/features/exam_attempt/speaking_writing/constants/speaking_writing_strings.dart lib/features/exam_attempt/presentation/widgets/exam_app_bar.dart lib/features/exam_attempt/presentation/widgets/exam_bottom_bar.dart` is clean (these 5 files have no forward-dependency on not-yet-relocated Phase 3 files).
- `task_type_dispatcher.dart` still declares `required this.audioPlayerService` and the switch statement has exactly 11 named-task-type arms (3 speaking + 8 listening) plus the reading/writing arms already present pre-merge, plus the `_` fallback.
- `task_type_dispatcher_test.dart` contains both the `REPEAT_SENTENCE routing` / `DESCRIBE_IMAGE routing` groups and a working `_MockAudioPlayerService`; test run is deferred to Phase 5 (file still references not-yet-relocated screen imports until Phase 3 lands).

## Risks
- Constant-naming collision between dev's read-aloud-specific and local's shared-recording strings: resolved explicitly by step 4's deletion — re-verify no other file still references the deleted `readAloud*` constants before finishing this phase (only `read_aloud_advance_button.dart`/its test did, and both are deleted in Phase 3, not fixed).
- `exam_app_bar.dart`'s full body replacement (dev's version, dropping local's `_BrandBlock`) is a product-visible change (no more 3-line brand block) — confirmed intentional per dev's own doc comment ("No force-submit affordance here anymore — removed per product decision" was the *old* comment; dev's version documents the force-submit re-add instead). Flag for user review in Phase 5's summary, do not silently drop without noting it.

- NOTED (plan-reviewer): The old-path `lib/features/exam_attempt/presentation/cubit/auto_record_cubit.dart` deletion (Phase 2 step 5) is grouped with already-conflict-free deleted files without explicitly noting it's one of this plan's own 11 marker-carrying files. Verified safe (its conflict is the same import-block-only rename-conflict as the new-path file resolved in Phase 2 step 1, byte-identical body) — Phase 2 step 5 should state this explicitly rather than imply it's a trivial already-resolved deletion.
- NOTED (plan-reviewer): `lib/features/exam_attempt/presentation/pages/write_essay_screen.dart` (the path *without* `writing/`) was in the originally-identified 20-file conflict list but does not exist on disk (only `presentation/pages/writing/write_essay_screen.dart` exists) — likely a duplicate/typo in the original list. No action needed; noted here purely for audit completeness.
