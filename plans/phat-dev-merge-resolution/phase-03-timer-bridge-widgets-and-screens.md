# Phase 3: Timer-Bridge, Status Widgets & 3 Screens Relocation

## Requirements
Port the timer-bridge mixin, the speaking-task status/image widgets, and the 3 full screens (`ReadAloudScreen`, `RepeatSentenceScreen`, `DescribeImageScreen`) into `speaking_writing/presentation/{widgets,pages}/`, delete dev's obsolete manual advance button and old-path duplicates, and execute the `AppStrings` → `SpeakingWritingStrings` call-site swap defined in Phase 1.

## Steps
1. Relocate `lib/features/exam_attempt/presentation/widgets/{auto_record_timer_bridge_mixin,auto_record_status_card,task_image_display,recorded_answer_status_card,audio_listening_status_card,instruction_text}.dart` to `lib/features/exam_attempt/speaking_writing/presentation/widgets/`, converting each file's relative imports to `package:pte_app/...` style and pointing at the Phase 2 cubit locations (`speaking_writing/presentation/cubit/{auto_record_cubit → read_aloud_cubit.dart file, auto_record_state}.dart`) and each file's own `AppStrings.*` references to the matching `SpeakingWritingStrings.*` constants added in Phase 1.
2. Relocate `lib/features/exam_attempt/presentation/pages/speaking/repeat_sentence_screen.dart` and `describe_image_screen.dart` to `lib/features/exam_attempt/speaking_writing/presentation/pages/`, converting imports to `package:pte_app/...` style (pointing at the Phase 2/step-1 relocated cubit and widget files) and swapping `AppStrings.*` → `SpeakingWritingStrings.*` — these have no dev counterpart to reconcile against, this is a pure move+import-fix.
3. Replace `lib/features/exam_attempt/speaking_writing/presentation/pages/read_aloud_screen.dart` (dev's old manual-button screen: phase-label `Text` + Start/Stop `PrimaryButton`, no timer-bridge, no `AutoRecordStatusCard`) entirely with local's full screen currently at `lib/features/exam_attempt/presentation/pages/speaking/read_aloud_screen.dart` (timer-bridge, passage display, `InstructionText`, `AutoRecordStatusCard`, `AutoAdvanceOnUploadReady`), same import-path and string-constant conversion as step 2.
4. Delete `lib/features/exam_attempt/speaking_writing/presentation/widgets/read_aloud_advance_button.dart` (dev's obsolete manual advance button, no conflict markers — clean delete, superseded by `AutoAdvanceOnUploadReady`) and `test/widget/features/exam_attempt/read_aloud_advance_button_test.dart` (resolve its import conflict moot by deleting the whole file).
5. Delete the now-fully-superseded old flat-path originals: `lib/features/exam_attempt/presentation/widgets/{auto_record_timer_bridge_mixin,auto_record_status_card,task_image_display,recorded_answer_status_card,audio_listening_status_card,instruction_text}.dart` and the whole `lib/features/exam_attempt/presentation/pages/speaking/` directory (all 3 screens).
6. Update `task_type_dispatcher.dart`'s import block (finalized in Phase 1, forward-looking) — confirm it now resolves cleanly against the physically-relocated `speaking_writing/presentation/pages/{read_aloud,repeat_sentence,describe_image}_screen.dart`; no further edit expected if Phase 1's guess was accurate, fix only if `dart analyze` disagrees.
7. Fix import paths (production-path only, no logic changes) in `test/widget/features/exam_attempt/{read_aloud_screen_test,repeat_sentence_screen_test,describe_image_screen_test,auto_record_status_card_test,task_image_display_test,audio_listening_status_card_test}.dart` to point at the new `speaking_writing/presentation/{cubit,widgets,pages}/` locations — files themselves stay at their current flat `test/` paths.

## Success Criteria
- `Glob lib/features/exam_attempt/presentation/pages/speaking/**` and `Glob lib/features/exam_attempt/presentation/widgets/{auto_record_timer_bridge_mixin,auto_record_status_card,task_image_display,recorded_answer_status_card,audio_listening_status_card,instruction_text}.dart` both return empty.
- `Glob lib/features/exam_attempt/speaking_writing/presentation/pages/*.dart` lists `read_aloud_screen.dart`, `repeat_sentence_screen.dart`, `describe_image_screen.dart`, `write_essay_screen.dart` (write_essay resolved in Phase 4, may not be clean yet).
- `lib/features/exam_attempt/speaking_writing/presentation/pages/read_aloud_screen.dart` contains `AutoRecordTimerBridgeMixin`, `AutoRecordStatusCard`, `InstructionText`, and `AutoAdvanceOnUploadReady` references — not `PrimaryButton`-driven manual Start/Stop.
- `lib/features/exam_attempt/speaking_writing/presentation/widgets/read_aloud_advance_button.dart` and `test/widget/features/exam_attempt/read_aloud_advance_button_test.dart` no longer exist.
- `grep -rn "AppStrings\." lib/features/exam_attempt/speaking_writing/` returns no matches (all speaking-task string references now go through `SpeakingWritingStrings`).
- **CRITICAL GATE**: `grep -n "pinnedItemPublicId" lib/features/exam_attempt/speaking_writing/presentation/widgets/auto_record_timer_bridge_mixin.dart` shows the identity guard inside `_forwardIfCurrentTask` still comparing against the screen's own task, and the doc comment above it (explaining `TaskTypeDispatcher`'s deferred-dispose/microtask race this guard exists to prevent) is present verbatim — diff against the pre-merge local file to confirm zero truncation.
- `dart analyze lib/features/exam_attempt/speaking_writing/` is clean (aside from anything still pending Phase 4's `write_essay_screen.dart`/dispatcher-adjacent files).
- `flutter test test/widget/features/exam_attempt/{read_aloud_screen_test,repeat_sentence_screen_test,describe_image_screen_test,auto_record_status_card_test,task_image_display_test,audio_listening_status_card_test}.dart` all pass (full-suite run deferred to Phase 5, but these can be spot-checked now since their dependencies are fully relocated by end of this phase).

## Risks
- Largest single-phase file count in this plan (6 widgets + 3 screens + 2 deletions + 6 test-import fixes) — highest chance of a copy-paste import mistake silently pointing at a deleted old-path file — Mitigation: run `dart analyze lib/features/exam_attempt/speaking_writing/` after every 2–3 files moved, not once at the end of the whole phase.
- `TaskImageDisplay`'s asset/network-image error-builder path and `AudioListeningStatusCard`'s sub-stage rendering (used only by `DescribeImageScreen`/`RepeatSentenceScreen` respectively) are easy to visually confirm compile but not easy to confirm behaviorally correct from a grep/analyze gate alone — Mitigation: rely on the existing widget tests (`task_image_display_test.dart`, `audio_listening_status_card_test.dart`) passing as the actual behavioral gate, not just `dart analyze`.
