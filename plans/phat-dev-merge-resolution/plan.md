# Plan: Resolve `git merge dev` into `phat/feat/listening-writing-ui`
Status: ✅ Complete (conflicts resolved — merge itself NOT finalized, see Session Notes)
Date: 2026-08-23
Mode: Hard

## Overview
A `git merge dev` is in progress (MERGE_HEAD present, merge-base `112ef999022efd925ae74b66d184eb409d4ac67b`) on `phat/feat/listening-writing-ui`. This plan resolves all 11 files carrying live conflict markers plus the associated non-conflicted-but-affected files (orphaned old-path duplicates, string call sites, tests), relocates this session's fully-automatic Read Aloud/Repeat Sentence/Describe Image work into dev's new `speaking_writing/` package structure, and gets `flutter analyze` clean and the full test suite green. It stops short of `git add -A && git commit` / `git merge --continue` — the user finishes the merge manually.

## Phases
- [x] Phase 1: Strings & Task-Type Dispatcher Hub — resolve `app_strings.dart`/`exam_attempt_strings.dart`/`speaking_writing_strings.dart` and `task_type_dispatcher.dart` (+ its test), the compile-blocking hub every screen depends on.
- [x] Phase 2: AutoRecordCubit/AutoRecordState Relocation — port the shared cubit, state, and upload-tracking primitives into `speaking_writing/presentation/cubit/`, with the CRITICAL re-entrancy-guard verification gate.
- [x] Phase 3: Timer-Bridge, Status Widgets & 3 Screens Relocation — port the timer-bridge mixin, status-card/image widgets, and `ReadAloudScreen`/`RepeatSentenceScreen`/`DescribeImageScreen` into `speaking_writing/`, delete dev's obsolete manual advance button, execute the `AppStrings` → `SpeakingWritingStrings` call-site swap.
- [x] Phase 4: Remaining Conflicts & Orphan Cleanup — dedupe `write_essay_screen.dart`, hand-merge `re_order_paragraphs_list.dart`, delete the superseded old-path `reading_task_preview_screen.dart`, verify the already-clean `reading/dev/` fixture files.
- [x] Phase 5: Final Regression & Handoff — `flutter analyze`, full `flutter test` (new baseline), `check-standards.sh`, explicit "merge not finalized" closing note.

## Research Summary
Direct inspection of the repo (not the task brief alone) confirms:
- Exactly **11 files** carry live `<<<<<<<`/`=======`/`>>>>>>>` markers today: `lib/core/constants/app_strings.dart`; `lib/features/exam_attempt/presentation/widgets/{task_type_dispatcher,exam_app_bar,exam_bottom_bar}.dart`; `lib/features/exam_attempt/presentation/cubit/auto_record_cubit.dart`; `lib/features/exam_attempt/presentation/pages/writing/write_essay_screen.dart`; `lib/features/exam_attempt/reading/presentation/widgets/re_order_paragraphs_list.dart`; `lib/features/exam_attempt/speaking_writing/presentation/{cubit/read_aloud_cubit,pages/write_essay_screen}.dart`; `test/widget/features/exam_attempt/{task_type_dispatcher_test,write_essay_screen_test}.dart`.
- Two files the task brief flagged as an "add/add conflict" — `lib/features/exam_attempt/reading/dev/{dev_attempt_repositories,speaking_writing_task_fixtures}.dart` — **have no conflict markers on disk right now**; they already contain local's fixtures/comments verbatim. Ground truth overrides the brief here: no edit needed, verify-only in Phase 4.
- `lib/features/exam_attempt/reading/dev/dev_attempt_repositories.dart`'s `Dev*Repository` classes have **zero consumers** post-merge — dev's rewritten `reading_task_preview_screen.dart` now drives previews off the real `getIt<ExamAttemptBloc>()` (wired in `main.dart`), not a throwaway dev repository stack. This is dead-but-harmless code outside the conflict set; left untouched, flagged as a note.
- `read_aloud_advance_button.dart` (new `speaking_writing/presentation/widgets/` path) and its test have **no markers** — they are dev's clean addition, to be deleted outright per the user's decision (superseded by `AutoAdvanceOnUploadReady`).
- `write_essay_screen.dart`'s conflict (both at the old `presentation/pages/writing/` path and the new `speaking_writing/presentation/pages/` path — git surfaces the same rename-conflict at both locations, same pattern as `auto_record_cubit.dart`/`read_aloud_cubit.dart`) is **import-block-only** — the class body is byte-identical on both sides. No behavioral merge needed, just take dev's `package:` import style and delete the old-path duplicate.
- `task_type_dispatcher.dart`'s switch-body conflict is a pure additive union (local's `REPEAT_SENTENCE`/`DESCRIBE_IMAGE` cases vs. dev's 8 Listening cases) — no case-name collision. Dev added a required `audioPlayerService` constructor param consumed only by Listening cases; must be preserved and threaded through unchanged.
- No test file references `AppStrings` directly (confirmed via repo-wide grep) — the string redistribution's test surface is zero; only production files need call-site updates.
- `test/` has no mirrored subdirectory structure for `lib/`'s `reading/`/`listening/`/`speaking_writing/` reorganization — dev kept tests flat under `test/unit/features/exam_attempt/` and `test/widget/features/exam_attempt/`. This plan follows that convention: only `lib/` files move, test files get import-path fixes in place.
- `instruction_text.dart` is consumed only by the 3 speaking screens (grep-confirmed, not by any reading screen) — safe to relocate into `speaking_writing/presentation/widgets/` alongside the other speaking-only widgets.

Chosen approach: keep local's fully-automatic `AutoRecordCubit`/timer-bridge/3-screen architecture verbatim (bodies untouched, only import paths and file locations change), keep dev's package structure and `package:pte_app/...` absolute-import convention, keep dev's `SpeakingWritingStrings`/`ExamAttemptStrings` split and extend both rather than introducing a third strings class.

## Dependencies
None external. Sequencing dependency only: Phase 2 must land before Phase 3 (screens import the relocated cubit/state), and Phase 1's `SpeakingWritingStrings` additions must land before Phase 3's call-site swap consumes them.

## Risks
- HIGH: The `pinnedItemPublicId` identity guard (`auto_record_timer_bridge_mixin.dart`) and the `_startInFlight`/`_stopInFlight` re-entrancy guards (`auto_record_cubit.dart`'s `onTimerSnapshot`) get silently dropped or truncated while hand-resolving import-block conflicts across many touched files — Mitigation: Phase 2 and Phase 3 each end with a dedicated grep-based verification step that both guards and their full doc comments are present verbatim at the new path, not just "it compiles."
- MEDIUM: `SpeakingWritingStrings` already has 6 constants (`readAloudStartRecordingLabel`, `readAloudStopRecordingLabel`, `readAloudRecordingIndicator`, `readAloudNotRecordedYetLabel`, `readAloudStillUploadingLabel`, `readAloudUploadReadyLabel`) written for dev's now-deleted manual-button screen; some names collide in meaning (not name) with local's generic `recording*`-prefixed constants meant to be shared across 3 task types — Mitigation: Phase 1 explicitly decides naming (keep local's generic `recording*` names since the status card is shared by 3 tasks, remove dev's now-orphaned read-aloud-specific manual-button strings since their only consumer is being deleted).
- MEDIUM: This repo has no single stable pre-merge test-count baseline (both sides added tests independently) — Mitigation: Phase 5's own full `flutter test` run is documented as the new baseline, not compared against a stale number.
- LOW: `reading/dev/dev_attempt_repositories.dart` is dead code post-merge (zero consumers) but not part of the conflict set — Mitigation: leave untouched, document as a note for dev's own team rather than deleting unrelated code as part of a merge-resolution plan.
- LOW: Renaming `speaking_writing/presentation/cubit/read_aloud_cubit.dart` to `auto_record_cubit.dart` for naming consistency with its `AutoRecordCubit` class (as was done for `read_aloud_state.dart` → `auto_record_state.dart`) was considered but not required by git's own rename resolution — Mitigation: keep the filename dev's rename target already established (`read_aloud_cubit.dart`) to minimize additional churn; revisit only if code review flags it.

## Validation Q&A (Step 4)
User confirmed both open points as-planned, no changes:
1. `exam_app_bar.dart` — take dev's version wholesale (force-submit + phase label + countdown), dropping local's `_BrandBlock` (PTE UNI / Test of English Academic / TESTING ACCOUNT) entirely rather than hand-merging both — approved. Can be re-added later as a separate task if needed.
2. Ready to cook the full 5-phase plan; user will run `git add -A && git commit` themselves once done — approved.

## Session Notes
<!-- Updated by cook automatically — do not edit manually -->

**Last active:** 2026-08-23
**Phase in progress:** phase-05-final-regression-and-handoff (complete) — plan complete
**Status:** All 5 phases done. Merge conflicts fully resolved, full suite green, staged and ready for the user to commit. Merge itself intentionally NOT finalized.

### Decisions made this session
- `app_strings.dart` → dev's reduced `appTitle`-only class, verified zero surviving consumers of the removed constants outside files this plan already deletes/relocates (`main.dart` only uses `appTitle`).
- `exam_attempt_strings.dart` needed **no edit** — `writeEssayTextFieldLabel` already covered by `SpeakingWritingStrings`, every other shell-level constant already present.
- `speaking_writing_strings.dart` rewritten: added all speaking/read-aloud/describe-image/repeat-sentence/recording/audio-listening constants (local's generic `recording*` names, since the status card is shared across 3 task types), removed dev's 6 orphaned manual-button-only constants (`readAloudStartRecordingLabel` etc. — their only consumer, dev's old manual screen, is deleted in Phase 3).
- `exam_app_bar.dart` → took dev's version wholesale per user's confirmed decision (force-submit + phase label + countdown, `_BrandBlock`/`_CountdownBlock` dropped).
- `exam_bottom_bar.dart` → deviated slightly from the plan's literal "union both import lists" instruction: dev's added `timer_phase.dart`/bloc imports are genuinely unused by this file's unchanged body (verified — would trigger `unused_import`), so only `AppColors`/`AppDimensions` were kept, in dev's `package:` style.
- `task_type_dispatcher.dart` → import union + switch-body union (local's `REPEAT_SENTENCE`/`DESCRIBE_IMAGE` + dev's 8 Listening cases) landed cleanly; the `audioPlayerService` field/constructor param had actually already auto-merged by git (non-conflicting region) — only a stray trailing `>>>>>>> dev` marker needed removing. Verified 17 switch arms (6 reading/writing + 3 speaking + 8 listening), matching expected count exactly.
- `task_type_dispatcher_test.dart` → both sides' additions (local's `_FakePathProviderPlatform`/task fixtures/routing groups, dev's `_MockAudioPlayerService`) were already non-overlapping; pure marker removal, no content changes needed. Full test run deferred to Phase 5 per this phase's own plan (file still imports not-yet-relocated Phase 3 screens).

- Phase 2: kept the dev-established filename `read_aloud_cubit.dart` at the new path (per the plan's own LOW-risk note) even though the exported class is still `AutoRecordCubit` — not renamed to `auto_record_cubit.dart`, to minimize churn as the plan intended. Rewrote `auto_record_state.dart`/`upload_tracking_state.dart`/`recording_phase.dart` wholesale from the reference local shape (not patched incrementally) — confirmed `AutoRecordState implements UploadTrackingState` at the new path, not the stale `ReadAloudState` dev had there. Deleted dev's old pre-unification `read_aloud_state.dart`, and all 6 old flat-path files (`auto_record_cubit.dart`, `auto_record_state.dart`, `upload_tracking_state.dart`, `recording_phase.dart`, `auto_advance_on_upload_ready.dart`, the very-old `read_aloud_cubit.dart`). Also fixed `auto_record_cubit_test.dart`'s stale `domain/audio_recorder_service.dart` import (old path no longer exists post-dev-reorg — only `speaking_writing/domain/audio_recorder_service.dart` does) — not explicitly called out in the phase file but required for compilation. Deleted the superseded `read_aloud_cubit_test.dart`.
- **CRITICAL GATE PASSED**: `grep -n "_startInFlight\|_stopInFlight"` on the new-path cubit shows both guard fields and both usages inside `onTimerSnapshot`, with the full "In-flight guards for the auto-record path..." doc comment present verbatim — confirmed byte-for-byte against the pre-merge local file.

- Phase 3: relocated all 6 widgets (`auto_record_timer_bridge_mixin`, `auto_record_status_card`, `task_image_display`, `recorded_answer_status_card`, `audio_listening_status_card`, `instruction_text`) and all 3 screens (`ReadAloudScreen`, `RepeatSentenceScreen`, `DescribeImageScreen`) into `speaking_writing/presentation/{widgets,pages}/`, with every `AppStrings.*` reference swapped to `SpeakingWritingStrings.*` in the same pass (not a separate later step). Dev's old manual-button `read_aloud_screen.dart` at the new path was fully replaced by local's timer-bridge-driven version; `read_aloud_advance_button.dart` + its test deleted outright (superseded by `AutoAdvanceOnUploadReady`). Deleted the old flat-path originals (6 widget files + the whole `pages/speaking/` directory).
- Fixed 6 test files' imports to the new `speaking_writing/` paths (`audio_listening_status_card_test.dart`, `auto_record_status_card_test.dart`, `task_image_display_test.dart`, `read_aloud_screen_test.dart`, `repeat_sentence_screen_test.dart`, `describe_image_screen_test.dart`) — files themselves stayed at their flat `test/` paths per dev's convention. All 6 spot-checked individually: 5/7/7/3/2/2 passing, matching exact expected counts.
- **CRITICAL GATE PASSED**: `pinnedItemPublicId` identity guard present verbatim in the relocated `auto_record_timer_bridge_mixin.dart`, including its full doc comment explaining the `TaskTypeDispatcher` dispose-timing race.
- Scoped `dart analyze lib/features/exam_attempt/speaking_writing/` shows 132 issues, all exclusively in `write_essay_screen.dart` (confirmed via `grep -v` filtering) — that file still carries its own unresolved conflict markers, which is explicitly Phase 4's scope per this phase's own gate note ("aside from anything still pending Phase 4's write_essay_screen.dart"). Everything else in the package is clean.

- Phase 4: `write_essay_screen.dart` (both paths) and `re_order_paragraphs_list.dart` resolved exactly as the plan predicted (import-block-only / single-line conflicts) — the `adjustedNewIndex` bug-fix confirmed preserved. Old-path `write_essay_screen.dart` and `reading_task_preview_screen.dart` deleted (confirmed zero remaining importers of the old `reading_task_preview_screen.dart` path via grep — `main.dart` already pointed at the new one), their now-empty parent directories (`presentation/pages/writing/`, `exam_attempt/dev/`) removed.
- **Real deviation from the plan found and fixed**: Phase 4 Step 6 called `reading/dev/dev_attempt_repositories.dart` and `speaking_writing_task_fixtures.dart` "verify-only, no edit expected" since they had zero conflict markers — true, but `dart analyze` revealed both files still carried **relative imports** (`../domain/...`) written for their *original* flat `exam_attempt/dev/` location; git relocated their content bodily to the new `reading/dev/` path (2 directories deeper) without adjusting those relative paths, so `../domain/` resolved to a nonexistent location and every type (`TaskView`, `TimerStateResponse`, `TimerPhase`, `AttemptTaskResponse`) was undefined. Fixed both files' imports to absolute `package:pte_app/...` style, matching the rest of this merge's convention. This is exactly the kind of gap "no markers" checks can't catch — worth remembering for any future merge-relocation work.
- Repo-wide final marker sweep: 0 matches for `<<<<<<<` anywhere in `lib/` or `test/`.
- Ran `git add -A -- lib/ test/` to stage every resolved/deleted path, clearing git's index-level unmerged markers (0 remaining `UU`/`AA`/`DD`/`AU`/`UD`/`UA`/`DU` entries) — per the plan's own explicit allowance ("`git rm`/`git add`/`git mv`... fine and expected as part of conflict resolution"). **No `git commit` or `git merge --continue` was run** — staging only, per the user's standing no-auto-commit policy; the user finishes the merge manually.

- Phase 5 (final): whole-repo `flutter analyze` — **0 issues**. Whole-repo `flutter test` first run: **276/278** (2 failures, both in `exam_app_bar_test.dart`).
  - **Root cause (not anticipated by any phase file)**: `exam_app_bar_test.dart` predated this merge and was never in the conflict list (it's an unmodified/non-conflicting file), but it asserted against local's OLD `ExamAppBar` shape (`ValueKey('examAppBarCountdown')` on a "Time Remaining hh:mm:ss" `_CountdownBlock` reading `examRemaining`) — a shape that no longer exists once Phase 1 took dev's `ExamAppBar` wholesale per the user's confirmed decision (phase label + `remaining` as mm:ss + force-submit, no stable key). Phase 1 flagged the *behavioral* change as a risk to surface to the user, but didn't flag that a specific pre-existing test would break — a real gap in the plan.
  - **Fix applied**: added back a minimal `key: const ValueKey('examAppBarCountdown')` on dev's countdown `Text` (zero behavior change, just a stable test seam) and updated the test's 2 expected values from the old `'Time Remaining 01:15:30'`/`'...01:15:12'` (hh:mm:ss of `examRemaining`) to dev's actual `'00:30'`/`'00:12'` (mm:ss of `remaining`) — matching `_formatRemaining`'s real behavior. Both tests pass.
  - Re-ran full suite after the fix: **278/278**, 0 failures. This is the new baseline (explicitly not compared to any stale pre-merge number, since neither side had one that survived independently — matches this plan's own stated approach).
  - Both CRITICAL-guard greps re-run one final time: **zero drift** from Phase 2/3 results — `_startInFlight`/`_stopInFlight` and the `pinnedItemPublicId` guard (with full doc comments) are present verbatim.
  - `check-standards.sh`: 4 hardcoded-string warnings + 1 file-length warning, all in files this merge never created/touched substantively (`listening_word_count_label.dart`, `task_type_dispatcher.dart`, `word_count_label.dart`, `skill_score_row.dart`, `app_database.g.dart`) — no new violations introduced.
  - Final orphan sweep: all 6 old cubit/widget files, `pages/speaking/`, `pages/writing/`, and `exam_attempt/dev/` confirmed absent. Final marker sweep: 0 matches for `<<<<<<<` anywhere in `lib/`/`test/`.
  - Re-staged the two Phase-5-touched files (`exam_app_bar.dart`, `exam_app_bar_test.dart`) via `git add -A -- lib/ test/` — confirmed 0 remaining unmerged (`UU`/`AA`/`DD`/`AU`/`UD`/`UA`/`DU`) entries anywhere.

**Merge is NOT finalized.** All conflicts are resolved, the working tree is staged (`git add`, not `git commit`), and the full suite is green. Run `git commit` yourself (or `git merge --continue` if that's the flow you're in) to complete the merge — per this project's "user commits their own code" policy. No `git commit`, `git push`, or `git merge --continue` was run at any point in this plan.

### Post-implementation verification (Hard mode Step 3/4)
- Independent `tester`-agent pass: confirmed 278/278 unchanged, checked for stale relative-import gaps repo-wide (none found beyond the 2 already fixed), confirmed both CRITICAL guards are exercised by real passing assertions (not just present). No fixes needed.
- `code-reviewer` pass: **APPROVED, 9.5/10, 0 findings** (CRITICAL/HIGH/MEDIUM/LOW all 0). Independently re-ran analyze/test, re-verified both CRITICAL guards, read all 3 relocated screens end-to-end confirming no regression toward dev's old manual-button pattern, confirmed the `ExamAppBar` test fix was a legitimate adaptation (not a weakened assertion hiding a regression). Two non-blocking notes for human judgment: (1) the `_BrandBlock` removal is a real product-visible change, confirmed user-approved — worth a final visual glance; (2) `reading/dev/dev_attempt_repositories.dart`'s dead `Dev*Repository` classes are a pre-existing, out-of-scope cleanup opportunity, not introduced by this merge.
- Hard mode requires explicit human approval (no auto-approve) — awaiting user sign-off before this plan is considered fully closed.

### Post-approval fix: dev-preview picker missing Speaking/Writing fixtures
User caught (via screenshot) that the running app's debug FAB only showed a book icon (Reading) and headphones icon (Listening) — no way to preview Read Aloud/Repeat Sentence/Describe Image at all. Root cause: `code-reviewer`'s note about `speaking_writing_task_fixtures.dart` having "no preview-screen consumer at all" was accurate but I hadn't connected it to a missing FAB — dev's rewritten `reading/dev/reading_task_preview_screen.dart` is actually generic (already fully wired for every task type via `TaskTypeDispatcher`, including `audioRecorderService`), but its picker `ListView` only iterated `ReadingTaskFixtures.all`, silently dropping local's pre-merge screen's `[...ReadingTaskFixtures.all, ...SpeakingWritingTaskFixtures.all]` merge. No new screen/route/FAB was needed — just restored that list merge (1-line fix) in the existing generic picker, matching the established pre-merge behavior exactly. Re-verified: `flutter analyze` clean (whole repo), full suite still 278/278, re-staged.

### Next immediate action
None — plan complete. Awaiting the user's manual `git commit`/`git merge --continue` to finish the merge.
